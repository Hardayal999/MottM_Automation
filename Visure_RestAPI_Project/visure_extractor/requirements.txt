# Visure API Extractor — Python dependencies
# Install with: pip install -r requirements.txt

requests         # HTTP client for talking to the Visure API
python-dotenv     # Loads credentials from .env file
pandas          # Tabular data handling (Element list -> DataFrame)
openpyxl          # Writes the .xlsx file (pandas uses this engine)
